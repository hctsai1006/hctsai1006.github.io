# legacy/terminal-v1.html — provenance

A frozen, byte-identical copy of the single-file terminal that this project is
being rewritten from. It exists so the rewrite has something to be *correct
against*: before this archive, `index.html` was the only specification of how
the site behaves, and a specification you are actively editing is not one.

Do not edit this file. If it needs to change, the archive is wrong.

| | |
| --- | --- |
| Source | `index.html` at the repository root |
| Commit | `0838080474d7e34d45cff9f242d2cdb7adde3380` |
| Git blob | `21794ce2250e2ec525eb146fcd688e93407ea90d` |
| SHA-256 | `c7f2943d3793ecf52aeb503f68e192a6fb190534391815bb96c47b2fe7689866` |
| Archived | 2026-09-04 |
| Lines | 2113 |

## What it is

One HTML file: markup and CSS in lines 1-197, and a single `<script>` block
containing the entire engine. It implements 67 commands, 46 aliases and 11
easter eggs over a virtual Unix filesystem persisted to `localStorage`, with a
nano/vim editor, tab completion, prediction, themes, and full IME support.

It has no build step and no dependencies, which is why it has survived.

## Why it is being replaced

Not because it is bad — it is dense, careful work. Because it has no seams:
four independent tokenizers, commands that write directly to the DOM, a
filesystem layer that returns rendered view objects, and no object model, so
`gci | Sort-Object` sorts formatted text including the `UnixMode` prefix
rather than sorting objects by a property.

## Verifying this archive

Compare the **git blob hash**, not the raw bytes. This repository has
`core.autocrlf=true`: the working tree is CRLF while git stores LF, so
`git show <commit>:index.html | cmp -` fails at byte 16 on a file that is in
fact unchanged. The blob hash is computed after normalisation and is therefore
the identity that actually holds on every machine.

```sh
[ "$(git hash-object index.html)" = "$(git hash-object legacy/terminal-v1.html)" ]   && echo identical
```

Both currently hash to `21794ce2250e2ec525eb146fcd688e93407ea90d`.

The recorded SHA-256 above is of the *working-tree* bytes on a CRLF checkout, so
it will differ on a machine that checks out LF. It is recorded for completeness;
the blob hash is the authoritative one.

The live site still serves `index.html`. This copy is the reference the
conformance suite replays against, and `index.html` is only replaced once that
suite reaches parity.
